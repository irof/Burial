plugins {
    id 'java'
    id "org.dddjava.jig-gradle-plugin" version "2018.10.4"
}

jigReports.dependsOn(clean, compileJava, processResources)

sourceCompatibility = '1.8'

repositories {
    jcenter()
}

dependencies {
}

jig {
    modelPattern = '.+\\..+'
}
